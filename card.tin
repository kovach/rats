!game.
!move foo bar.

?game / (!setup; !turn ~ !initial_turn).

?setup / (
  (!card / !name village | !action) |
  (!card / !name gold | !treasure) |
  !place !P
).


>?place P |
>(?card / (?name A | ?action)) |
>(?card / (?name B | ?treasure)) |
( ?initial_turn / (
  !move A P | !move B A
)).

#card.
#action.
#treasure.
#move.
