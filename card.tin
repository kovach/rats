!game.

?game / (!setup; !turn).

?setup / (
  (!card / (!name !N1 | !action)) |
  (!card / !name !N2 | !treasure)
).

?turn / (
  (>?card | !asdf)
).

(?card / ?name _) ~ !bar.

>(?card / ?action) / !ok.
>(?card / ?treasure) / !ok2.
