!game.

?game / (!setup; !turn).

?setup / (
  (!card / (!name !N1 | !action)) |
  (!card / !name !N2 | !treasure)
).

?turn,
>(?card / (?name A | ?action)),
>(?card / (?name B | ?treasure))
/ (
  !asdf; !asdf
).

#asdf.
#card.
#action.
#treasure.
#name.

(?card / ?name _) ~ !bar.

>(?card / ?action) / !ok.
>(?card / ?treasure) / !ok2.
